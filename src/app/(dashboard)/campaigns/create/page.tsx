import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Create Campaign' };

export default function CreateCampaignPage() {
  return (
    <div>
      <h1>Create Campaign</h1>
      <p>Campaign creation form will be displayed here.</p>
    </div>
  );
}
